# 引き継ぎ

このセッションを引き継ぐ人／エージェント向け。README は「何を作ったか」、
これは「いまどうなっていて、次に何を踏むか」。

---

## これは何か

平日19時台の「今日の夕食どうする？」を1往復で終わらせるアプリ。
札幌すごいAIハッカソン2026夏（テーマ：顔を知る、たった一人の悩みを突き刺せ）。

対象は開発者の妻。**架空のペルソナではなく実在の一人**で、出発点は実際のLINE履歴。
19:18〜19:20の2分間に22通、決定打（冷蔵庫の残り物）が出たのは20通目。

課題の構造：**判断材料が夫と妻に2つずつ分かれていて、どちらも全体を見ていない。**
決定打を持っていた側も、それが決定打だと知らなかった。20往復は交渉ではなく探索。

---

## いまの状態

| | |
|---|---|
| 本番 | https://sapporo-ai-hackathon-2026-summer.vercel.app/ |
| GitHub | https://github.com/jacobiIdentity/sapporo-ai-hackathon-2026-summer （public） |
| HEAD | `a240dae` |
| 検証 | テスト77件 / lint / build すべてグリーン、作業ツリークリーン、本番全経路確認済み |

デモ用URL：

```
?h=now&l=1   2つ答えただけで決着する例（今日の芯）
?demo=1      LLMを呼ばず固定結果を返す（回線が不安なとき）
```

`?demo=1` は自由文の読み取りとレシート読み取りの**両方**に効く。
**発表でレシートを見せるときは `?demo=1` を使うこと。** 理由は下の「読み取りの精度」。

---

## 設計の芯（壊さないこと）

```
自由文   ──[LLM · 実行時]──▶ 4項目 ──[ルール]──▶ 結論 ──[表]──▶ 一文
レシート ──[LLM · 実行時]──▶ 材料                                 ↑
         非決定要素はこの2つだけ           ビルド時に生成・レビュー・固定
         外しても手で選べる／消せる
```

**1. 決定にLLMを入れない。**
結論を出すのは `lib/decide.ts` の純粋関数。同じ入力なら必ず同じ出力。
再現性はここでは機能要件で、夕食の判断がブレたら一度で信用を失う。

**2. LLMは境界だけ。**
入口は2つとも実行時。`/api/parse`（自由文→4項目、`claude-opus-5`）と
`/api/receipt`（レシート画像→材料、`claude-haiku-4-5`）。出口（結論→一文）はビルド時。
どれが落ちても、4つを手で選べば結論は出る。

**3. 出口は表を引くだけ。**
入力24通り・結論7通りなので `lib/sentences.json` に事前生成して固定してある。
**`decide.ts` の headline / reason / actions を変えたら再生成が必須。**
キーが変わって表が欠け、`lib/sentence.test.ts` が落ちるので気づける。

**4. `resolve()` は `decide()` の上に乗る層。**
未回答の項目を全展開（最大24通り）して、結論が収束していれば残りを聞かない。
割れていれば候補を最も減らす1問を名指しする。
**判断ルールそのものは `decide()` にあり、`resolve()` は触らない。**

**5. レシートは材料だけを埋める。残り物には繋がない。**
`/api/receipt` が読み取るのは買ったもの。何を作って何が残っているかは写らないので、
決定打である `leftovers`（できている料理）には触らない。ここを繋ぐと嘘の在庫で夕食が決まる。
**画面の NOT TODAY セクションは意図的に元のまま**（無効の「レシートを撮る」ボタンも残してある）。
あの宣言は「3つとも自動で埋まる」ことについてで、レシートが埋めるのは1つだけだから、
まだ達成していない。矛盾に見えても直さないこと。

**6. 在庫は「触ったときだけ」回答に落ちる。**
「できている料理」を編集した瞬間だけ `leftovers` に反映する。初期表示では同期しない
（URLに載っている回答を上書きしてしまうため）。反映は編集イベント1回きりなので、
在庫が空でもトグルは手で押し直せる。デモでは料理を3つ消すと結論がその場で変わる。

**7. URLが状態。** DB・認証・リアルタイム同期なし。共有はURLを送ること。

**8. 依存を増やさない。** UIライブラリ・状態管理・ORM・アイコン等は入れていない。

---

## ファイル

```
lib/decide.ts               判断ルール（decide）と問い合わせ層（resolve）、keyOf
lib/sentence.ts             結論 → 固定の一文
lib/sentences.json          事前生成した7件。人がレビューしてコミットするもの
lib/rate-limit.ts           スライディングウィンドウ、clientKey、tooMany（2ルート共通）
lib/receipt.ts              レシート読み取り結果の正規化
lib/image.ts                写真の縮小（canvas）。fitWithin は純粋関数
app/page.tsx                searchParams を読む Server Component
app/dinner-decider.tsx      画面本体（唯一のClient Component）
app/api/parse/route.ts      自由文の読み取り。実行時LLM（claude-opus-5）
app/api/receipt/route.ts    レシート画像の読み取り。実行時LLM（claude-haiku-4-5）
scripts/build-sentences.mjs 一文の再生成（開発時のみ）
```

テストは `lib/*.test.ts` と `__tests__/` に同居、計77件。

---

## コマンド

```bash
npm run dev
npm test && npm run lint && npm run build   # 変更のたびに全部

# decide.ts の文言を変えたら必ず
node --experimental-strip-types scripts/build-sentences.mjs

npx vercel deploy --prod
```

---

## つまずきポイント（既に踏んだもの）

- **`vercel deploy --prod` が権限分類器で止まることがある。**
  止まったら人間に `!npx vercel deploy --prod` を実行してもらう。
- **SSH push は不可。** ローカル鍵とGitHub登録鍵が不一致。origin は HTTPS にしてあり、
  `gh auth git-credential` で通る。SSHに戻さないこと。
- **ルートを削除したら `rm -rf .next`。** 削除済みルートの型がキャッシュに残りビルドが落ちる。
- **`vitest` は `globals: false`。** 自動クリーンアップが走らないので、
  レンダリングするテストには `afterEach(cleanup)` が必須。無いと前のテストのDOMが残る。
- **`scripts/*.mjs` は `node --experimental-strip-types`。** ビルドパイプラインの外。
- **レシート読み取りは外す。** 同じレシートで観測4回のうち、正確2回・品目の欠落1回・
  **レシートに無い品目の捏造1回**（「卵焼き」）。チップは×で消せるので壊れはしないが、
  発表で実レシートを読ませるのは賭けになる。`?demo=1` を使うこと。
  精度が要るならこのルートだけ `claude-opus-5` に上げる（0.45円→2.2円、実測で3回とも正解）。
- **Haiku 4.5 は `output_config.effort` 非対応。** 付けると400。代わりに構造化出力
  （`output_config.format`）で形を固定してある。`image_input` は対応。
- **レート制限が先に効くので本番で400を確認できないことがある。** `/api/receipt` は
  4回/分。curlを連続で叩くと5回目から429になる。入力検証のテストはユニット側にある。
- **コミット著者は noreply に書き換え済み。** `git config user.email` はこのリポジトリで設定済み。
  グローバルは旧アドレスのままなので、他リポジトリでは注意。

---

## 事実の注意（2回間違えた箇所）

- **「実家の残り物」ではなく「冷蔵庫の残り物」。** 決定打の置き場所を最初に誤り、
  コード・プロンプト・README・公開資料まで広げてしまった。
- **情報の持ち主は3人ではなく2人。** 子どもは2歳で判断材料を持たない。
  子は情報源ではなく、「もう限界」に交渉の猶予がないという**制約**。

新しい事実を受け取ったら、実装に入れる前に一度確認すること。

---

## 残タスク

**1. force push 後の旧コミット**
GitHub に未参照オブジェクトとしてSHA直打ちで一時的に残る。時間経過でGCされる。
急ぐなら GitHub Support に依頼。

**2. NOT TODAY の残り（意図的に未実装）**
- **音声で買ったものを登録** — レシート撮影は実装した（`/api/receipt`、材料だけ埋まる）。
  音声はまだ。画面には無効のモックだけ置いてある。
- **できている料理の自動登録** — 決定打はここだが、レシートには写らない。
  埋める手段が無いので着手していない。NOT TODAY の「3つが自動で埋まる」はこれが理由で未達。
- **17:00のプッシュ通知** — Web Push は購読情報の保存先が要り、iOS はホーム画面追加が必須。
  今日は iPhone のショートカットで代用した。

**これらは「できなかった」ではなく「条件を満たしてから作る」。** 画面の
NOT TODAY セクションはその宣言なので、消さないこと。

---

## 長期の方向

いま聞いている4項目のうち2つは、家の段取りの不具合の症状に過ぎない。

| 質問 | 消し方 |
|---|---|
| 米を炊いたか | 予約炊飯にすれば常に炊けている |
| 子の空腹度 | 17時に決めれば、まだ空いていない |
| 冷蔵庫の残り物 | 買ったものを登録すれば自動で埋まる |
| 寄り道できるか | 位置情報で自動で埋まる |

**ゴールは、このアプリが何も聞かなくなること。** 機能を足すのではなく質問を減らす方向に進める。

---

## 参考資料

- 課題と仕組みの説明: https://claude.ai/code/artifact/ace4534e-a6f1-40c4-9c6b-73ac21c65767
- 技術選定と開発の進め方: https://claude.ai/code/artifact/4fa6ea7a-cf05-4866-a2cc-486df76d13a8
