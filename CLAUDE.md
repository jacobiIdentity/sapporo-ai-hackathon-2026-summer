@AGENTS.md

# CLAUDE.md

札幌すごいAIハッカソン2026夏 用のNext.js基盤。テーマ確定前のスケルトン。

## 技術スタック（固定）

- Next.js App Router / TypeScript / Tailwind CSS / npm
- 追加ライブラリは原則導入しない。標準機能と既存依存で解けないか先に確認する。

## スコープの決め方

- 「一人のユーザー、一つの問題、一つの主要導線」に絞る。
- 機能数より、最後まで動くデモを優先する。
- 不要な抽象化を作らない。設定・インターフェース・レイヤーは実装が2つ以上必要になってから。
- 認証とDBは、テーマの中心価値の実現に必要な場合だけ追加する。それ以外はメモリ内状態か`localStorage`で済ませる。
- テーマが提示されるまで、テーマ固有の画面・APIは実装しない。
- ターゲット、提供価値、削る機能のいずれかが曖昧なら、実装せず人間に確認する。

## 実装ルール

- 新しい振る舞いを足すときは、外部から見た仕様を固定するテストを先に書く。
- 実装詳細ではなく、外部から観測できる振る舞いを検証する。
- データを表示する画面は、正常・ローディング・空・API失敗の4状態をすべて扱う。
- 秘密情報は`.env.local`に置く。Gitへ登録しない（`.gitignore`で`.env*`を除外済み）。

## 変更後に必ず実行する

```bash
npm run lint
npm run build
```

関連するテストがあれば併せて実行する。いずれかが失敗した状態で完了報告しない。

## 当日の運用

- 2026-08-29 14:10〜16:10 JST は発表スライドを作らない。必要な情報は`README.md`か別のMarkdownに記録する。
- コンテキスト圧縮が起きるときは、変更ファイル一覧・検証コマンド・デプロイURLを必ず残す。
